name: Condition DEMo
on: 
  push:
    branches:
      - main
      - develop
jobs:
  condition-demo:
    runs-on: ubuntu-latest
    steps:
      - name: checking if condition
        if: github.ref_name == 'main'
        run: echo "this condition is true"
      - name: this is whithout condition
        run: echo "always run"