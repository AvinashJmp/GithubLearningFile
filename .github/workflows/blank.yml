name: input-demo1
on:
  workflow
    inputs:
      environment:
        description: choose the input option
        required: true
        default: dev
  jobs:
    input-demo:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy an APP
          runs: echo "Deploying an application ${{inputs.environment}}"
